# NeonSmash — Nightly Backlog

Offene Aufgaben für Night Shifts, nach Priorität. Nicht jeder Eintrag ist eine Entscheidung — Status ist vermerkt.

## Aus Night Shift 2026-08-25 (manueller Lauf) hinzugekommen

- **[CLEANUP]** `DevicePerformance.DetectLowEnd()`: toten `OpenGLES2`-Vergleich entfernt (CS0618-Warnung). Unity unterstützt OpenGL ES 2.0 seit Version 2023.1 nicht mehr — `SystemInfo.graphicsDeviceType` kann diesen Wert also auf keinem Gerät mehr zurückgeben, der Vergleich war seitdem permanent `false` und damit funktional wirkungslos (kein Verhaltensunterschied durch die Entfernung, nur die Low-End-Erkennung via `graphicsMemorySize < 2048` bleibt als tatsächlich wirksames Kriterium übrig — das war schon vorher der Fall). Kommentar ergänzt, der erklärt, warum der Check entfernt wurde (nicht offensichtlich ohne Kontext). Kompiliert fehlerfrei, keine Warnung mehr. `AdManager.cs`s `MobileAds.RaiseAdEventsOnUnityMainThread`-Warnung (die andere nicht-IAP-Fund von eben) bewusst NICHT angefasst — betrifft Threading-Semantik für Ad-Callbacks in Produktions-Revenue-Code, laut Code-Kommentar direkt dafür verantwortlich, dass Callbacks sicher Unity-APIs berühren dürfen; eine Änderung ohne echtes Ad-SDK-Testing ist zu riskant für einen Night-Shift-Nebenbei-Fix.

- **[CLEANUP]** Alle 5 bereits vor dieser Night Shift bestehenden CS0414-Compiler-Warnungen ("assigned but never used") aufgeräumt: `GravityPoint.suckSpeed`/`shrinkSpeed`/`fadeSpeed` (per `grep` bestätigt: nirgends im File gelesen — die tatsächliche Suck/Shrink/Fade-Animation nutzt stattdessen `suckStartDistance`/`maxSuckForce`/`minScaleBeforeDestroy` über einen Lerp/Ease-Ansatz, vermutlich Überbleibsel einer älteren linearen Implementierung), `LevelHUD.currentLevel` (nie gelesen, kein SerializeField) und `LeaderboardPanelController.startAroundPlayer` (nie gelesen; das tatsächlich verwendete Feld für dieselbe Funktion heißt `shouldScrollToPlayer` und ist vollständig verdrahtet — `startAroundPlayer` war ein toter, offenbar durch `shouldScrollToPlayer` ersetzter Namens-Vorläufer). Alle drei Felder waren `private` ohne `[SerializeField]`-Inspector-Exposition bei `LevelHUD`/`LeaderboardPanelController`; `GravityPoint`s drei Felder waren `[SerializeField]`, aber ohne jeden Lesezugriff im Code funktional wirkungslos, unabhängig vom im Inspector gesetzten Wert. Kompiliert fehlerfrei, `compilationLogs` jetzt leer (keine der 5 Warnungen mehr vorhanden).

- **[BUG, gefunden & gefixt, niedrigere Schwere]** Beim Vergleich der drei `*ModeActivationPoint.cs`-Dateien (Fortsetzung des Fountain-Special-Mode-Fundes oben): `FountainModeActivationPoint.FinishCombo()` fehlte — im Gegensatz zu `GravityModeActivationPoint`/`VortexModeActivationPoint` — der `spawner.ForceClearCurrentPoint()`-Aufruf (löst den gerade aktiven normalen Tap-/Swipe-Point sofort aus/entfernt ihn) sowie der `IsModeActive`-Guard vor `StartMode()`. `ForceClearCurrentPoint()` ist nur lesend geprüft worden (`MixedPointSpawner.cs` selbst ist aktuell WIP/uncommitted und wurde NICHT verändert — nur die bereits öffentliche, unveränderte Methode von außen aufgerufen). Niedrigere Schwere als der Haupt-Fund: `FountainModeSystem.Activate()` ruft ohnehin kurz danach `spawner.ClearAllGameplayPoints()` (breiterer Clear, laut Code-Kommentar "wie bei Gravity"), daher wahrscheinlich nur ein sehr kurzes Zeitfenster betroffen, in dem ein alter Punkt sichtbar/tappbar bleiben könnte, statt eines dauerhaften Bugs. Fix: exakt dasselbe Muster wie bei Gravity/Vortex übernommen (`if (spawner != null) { if (... && !IsModeActive) StartMode(...); spawner.ForceClearCurrentPoint(); }`). Kompiliert fehlerfrei. **⚠️ NICHT VOLLSTÄNDIG GETESTET** — gleicher Play-Mode-Verifikationsbedarf wie beim Haupt-Fund oben.

- **[SCHWERWIEGENDER BUG, gefunden & gefixt]** `FountainModeSystem.cs` hatte — im Gegensatz zu `GravityModeSystem.cs` und `VortexModeSystem.cs` — KEINE `OnEnable`/`OnDisable`-Subscription auf `SpecialModeManager.OnModeStarted`. Gefunden durch systematischen Vergleich aller drei Special-Mode-System-Dateien (gleicher Ansatz wie beim Diamond/Splinter-Fund). Ablauf-Analyse bestätigt: `FountainModeActivationPoint.FinishCombo()` ruft nur `SpecialModeManager.Instance.StartMode(SpecialMode.Fountain)` auf (kein direkter `Activate()`-Call irgendwo im Projekt, `grep` bestätigt). `StartMode()` selbst feuert nur das `OnModeStarted`-Event — ohne eigenen Listener hätte `FountainModeSystem.Activate()` also NIE aufgerufen werden können. **Praktische Auswirkung: Der Fountain Special Mode wäre über den normalen Spielfluss (Orb spawnen → antippen → `FinishCombo()`) komplett funktionslos gewesen** — `currentMode` wechselt zwar auf `Fountain` (der Spielzustand "denkt" der Modus läuft), aber es hätten nie Fountain-Punkte gespawnt, und `OnSpecialPhaseComplete` (von `PhaseManager` abonniert, um zur nächsten Phase zu schalten) wäre nie gefeuert — vermutlich ein Soft-Lock der Phasenprogression, sobald der Fountain-Modus getriggert wird. Fix: identisches `OnEnable`/`OnDisable`/`HandleModeStart`-Muster wie bei Gravity/Vortex ergänzt (`if (mode == SpecialMode.Fountain) Activate();`). Kompiliert fehlerfrei. **Verifikation:** Versuch einer echten Funktionsprüfung im Editor (temporäre `SpecialModeManager`+`FountainModeSystem`-GameObjects erzeugt, `StartMode(Fountain)` aufgerufen, `IsActive` geprüft) zeigte KEINE Aktivierung — ABER ein Kontroll-Test mit dem etablierten, vermutlich funktionierenden `GravityModeSystem`-Muster zeigte exakt dasselbe Ergebnis (`IsActive` bleibt `False`). Das bestätigt: die Testmethode selbst funktioniert im Edit-Mode-Scripting-Kontext nicht zuverlässig (vermutlich `OnEnable` wird bei `AddComponent()` außerhalb des Play Mode nicht synchron ausgeführt) — kein Hinweis auf einen Fehler im Fix. Der Fix macht `FountainModeSystem` strukturell identisch zum bereits produktiv laufenden Gravity/Vortex-Muster. **⚠️ NICHT VOLLSTÄNDIG GETESTET** — bitte im Play Mode einmal real verifizieren: Fountain-Phase erreichen (oder Orb antippen), prüfen ob Fountain-Punkte tatsächlich spawnen. Alle temporären Test-GameObjects wurden bereinigt (verifiziert, 0 verbleibend).

- **[VERBESSERUNG, gefunden & gefixt]** Gleiches Guard-Muster wie beim Diamond-Fix oben auch bei `DreamEnergyManager.TrySpendDreamEnergy(amount)` ergänzt (`if (amount <= 0) return false;`). Anders als bei den Diamant-Methoden hat diese Methode zwei echte Aufrufer (`ShopInventory.TryPurchase`/`TryExchangeForCurrency`) — beide guarden aber bereits selbst gegen `dreamEnergyPrice <= 0`/`> 0` vor dem Aufruf, daher aktuell ebenfalls nicht praktisch ausnutzbar, nur Defense-in-Depth für den Fall künftiger neuer Aufrufer. Projektweite Suche (`grep -rn "TrySpend\|TryDeduct"`) bestätigt: Das waren alle drei existierenden "Spend"-Methoden im Projekt — alle drei jetzt mit demselben Guard versehen. Kompiliert fehlerfrei.

- **[BUG, gefunden & gefixt, latent]** `DiamondManager.TrySpendDiamonds(amount)` und `DiamondSplinterManager.TrySpendSplinters(amount)` hatten keine Guard-Klausel gegen `amount <= 0` (im Gegensatz zu `AddDiamonds`/`AddSplinters`, die das bereits korrekt prüfen). Bei negativem `amount` wäre `Balance < amount` immer `false` (Balance ist nie negativ), wodurch `Balance - amount` die Balance tatsächlich ERHÖHT hätte — ein "Spend"-Aufruf mit negativem Wert hätte also Diamanten/Splitter gutgeschrieben statt abgezogen. Per `grep` bestätigt: **beide Methoden werden aktuell nirgends im Projekt aufgerufen** (toter Code) — kein akut ausnutzbarer Bug, aber ein Fix jetzt verhindert, dass der Bug live wird, sobald irgendwo eine Diamanten-Ausgabe-Funktion (z. B. im Shop) auf diese Methoden aufgebaut wird. Fix: `if (amount <= 0) return false;` am Anfang beider Methoden ergänzt, analog zum bereits etablierten Muster in `AddDiamonds`/`AddSplinters`. Kompiliert fehlerfrei. Gefunden durch systematischen Vergleich der beiden strukturell identischen Zwillings-Dateien (`DiamondManager.cs`/`DiamondSplinterManager.cs`) gegeneinander.
- **[BEOBACHTUNG]** Da `TrySpendDiamonds`/`TrySpendSplinters` aktuell keine Aufrufer haben: Diamanten scheinen im Projekt aktuell nur ERWORBEN (IAP, Cloud-Load), aber nirgends aktiv AUSGEGEBEN zu werden (Splitter werden laut `ShopItem.cs` gegen Dream Energy getauscht, nicht umgekehrt). Möglicherweise ein noch nicht implementierter Diamanten-Ausgabepfad (z. B. Skin-Kauf mit Diamanten statt IAP) — nicht weiter verfolgt, da unklar ob das Absicht oder eine offene Baustelle ist. Für den User als Hinweis vermerkt.

- **[BUG, gefunden & gefixt]** `LeaderboardPanelController.RefreshAsync()` hatte KEIN try/catch um `GetMyScoreAsync`/`GetScoresAsync` — im Gegensatz zum sehr ähnlichen `LeaderboardController.cs`, der die äquivalenten Calls bereits korrekt in try/catch wrapped (gefunden beim Vergleich beider Leaderboard-Controller). Bei einem Netzwerkfehler während des Öffnens des Leaderboard-Panels (z. B. Timeout) hätte die Exception den `async Task` unbeobachtet abbrechen lassen — `loadingOverlay` wäre für immer sichtbar geblieben, `scrollRect.enabled` dauerhaft `false`: das Panel wäre bis zum Schließen/Neuöffnen softlocked gewesen, ohne Fehlermeldung außer einem generischen Unity-Konsolen-Error. Fix: try/catch um den riskanten Teil, im Fehlerfall `loadingOverlay` ausblenden + Scroll reaktivieren (identisches Cleanup-Muster wie im bereits vorhandenen `!online`-Fall direkt darüber) + `Debug.LogWarning`. Kompiliert fehlerfrei (`isCompilationSuccessful: true`). NICHT mit echtem simuliertem Netzwerkfehler im Play Mode getestet (bräuchte gezielte Netzwerkunterbrechung während des Panel-Öffnens) — Code-Pfad ist aber strukturell identisch zum bereits bewährten Muster in `LeaderboardController.cs`.

- **[VERBESSERUNG, gefunden & gefixt]** `FirebaseNotificationManager.RegisterTokenCoroutine()` baute das JSON-Payload für den FCM-Token-Registrierungs-Call per roher String-Konkatenation (`"{\"playerId\":\"" + playerId + ...`) statt richtiger Serialisierung — fragil, falls `playerId`/`token` je ein `"` oder `\` enthalten würden (aktuell unwahrscheinlich, da beide typischerweise alphanumerische/base64-artige UGS-/FCM-Strings sind, aber kein Format-Garant). Ersetzt durch `JsonUtility.ToJson` mit einem kleinen `[System.Serializable] struct RegisterTokenPayload`. Kompiliert fehlerfrei (`isCompilationSuccessful: true`). NICHT gegen den echten `RegisterTokenUrl`-Endpoint getestet (Cloud-Function-Aufruf, kein sicherer Weg das im Editor ohne echten Seiteneffekt zu verifizieren) — JSON-Output-Form ist aber äquivalent zum vorherigen String (`{"playerId":"...","fcmToken":"..."}`), sollte serverseitig unverändert ankommen.

- **[QA, Umfang eingegrenzt]** Nachdem der Alpha-Kanal-Fund am 1024er-iOS-Icon gemacht wurde: alle übrigen Icon-Assets (Android 512/192/144, iOS 120/180) ebenfalls per `sips -g hasAlpha` geprüft — **alle haben ebenfalls einen Alpha-Kanal**, das ist dort aber normal/erwartet (Android Adaptive Icons nutzen Alpha für Masking, kleinere iOS-Device-Icons werden von iOS selbst maskiert/abgerundet und Apple prüft dort keinen Alpha-Kanal). Nur der 1024×1024-"App Store"-Marketing-Slot ist tatsächlich betroffen (siehe Eintrag oben) — kein projektweites Icon-Problem, bewusst nicht auf alle Icons ausgeweitet. Zusätzlich `ScoreManager.cs` vollständig gelesen (117 Zeilen) — keine Auffälligkeiten (kein Overflow-Risiko in der Praxis relevant, negative Scores durch Mystery-Box-Minuspunkte sind laut Kommentar beabsichtigtes Design).

- **[ERLEDIGT, Regressionscheck]** Komplette EditMode-Testsuite (alle Assemblies, nicht nur `ComboManagerTests`) über `TestRunnerApi` laufen lassen: **25/25 Passed, 0 Failed, 0 Skipped**. Bestätigt, dass der heutige `MultiplayerManager.cs`-Fix (Commit `d8ca51b5`) keine bestehenden Tests gebrochen hat und die gesamte Suite (20 ältere Cloud-Save-Manager-Tests + 5 `ComboManagerTests`) weiterhin grün ist.

- **[BEOBACHTUNG, bewusst nicht selbst gefixt]** `IAPManager.ProcessPurchase()` geprüft: Belohnung wird korrekt VOR `PurchaseProcessingResult.Complete` vergeben (sicherer als umgekehrt — kein Verlust bei Absturz zwischen Reward und Store-Bestätigung). Aber: Wenn `FindItem(productId)` `null` liefert (kein passendes `ShopItem`-Asset für die productId gefunden — z. B. bei Store/Projekt-Konfigurationsdrift), wird trotzdem `Complete` zurückgegeben und der Kauf beim Store bestätigt, OHNE dass irgendeine Belohnung vergeben wird — nur eine `Debug.LogWarning`. In der Praxis würde das bedeuten: Spieler bezahlt echtes Geld, bekommt nichts, keine harte Fehlermeldung im Spiel, nur ein stiller Log-Eintrag. Aktuell nur ein theoretisches Risiko (Store-Produkt-IDs und `ShopItem`-Assets sollten synchron sein, kein akuter Hinweis auf eine tatsächliche Diskrepanz gefunden). **Bewusst nicht selbst verändert** — Änderungen an echter Zahlungs-/Kauf-Bestätigungslogik ohne Möglichkeit eines echten Store-Testkaufs sind zu riskant für eine eigenständige Night-Shift-Änderung. Empfehlung für eine spätere, mit echtem Sandbox-Kauf testbare Iteration: bei `item == null` zusätzlich eine sichtbare Fehlermeldung/Support-Hinweis im UI zeigen und/oder `NeonAnalytics` einen dedizierten "IAP-Reward-Mismatch"-Event loggen, damit sowas nicht unbemerkt bliebe.

- **[VORSICHT, bitte prüfen]** Um `DemoDesignScene.unity` auf Fehlerfreiheit zu prüfen, wurde `EditorSceneManager.OpenScene(..., OpenSceneMode.Single)` aufgerufen — das wechselt die im laufenden Unity-Editor **aktiv geöffnete Szene**. Vorher wurde NICHT geprüft, welche Szene offen war bzw. ob dort ungesicherte Änderungen im Editor-Speicher lagen, die über den bereits auf der Platte liegenden uncommitteten Stand hinausgehen (z. B. eigene manuelle Bearbeitung von `GameScene_InfinityMode`/`MainMenuScene`, die noch nicht einmal lokal gespeichert war). Kein Fehler/Dialog wurde beim Aufruf gemeldet, was dafürspricht, dass keine ungesicherten Änderungen verloren gingen — aber das ist keine Bestätigung. **Bitte beim nächsten Öffnen von Unity kurz prüfen, ob alle eigenen manuellen Szenen-Änderungen an `GameScene_InfinityMode.unity`/`MainMenuScene.unity` noch wie erwartet vorhanden sind.** Für zukünftige Night Shifts: vor jedem programmatischen Szenenwechsel per MCP zuerst `EditorSceneManager.GetActiveScene()` und `.isDirty` prüfen, nicht blind wechseln.
- **[ERLEDIGT, Doku-Korrektur]** `DemoDesignScene.unity` per Editor-Load geprüft: lädt fehlerfrei (0 neue Errors, nur 5 vorbestehende, unabhängige CS0414-Warnings). Dabei festgestellt: Die Backlog-Notiz vom 2026-08-21 ("`LeafGem_Boost`/`StoneShard_Boost` noch nicht in `DemoDesignScene.unity` platziert") ist **veraltet/falsch** — beide sind bereits in der Szene vorhanden (vermutlich durch Commit `02e76d9e`, "4 neue 3D-Concept-Assets platziert", zwischenzeitlich erledigt). Einzig `PrismShard_Boost` fehlt tatsächlich noch (Modell nie erstellt, nur leerer Stub-Ordner, siehe Eintrag unten).

- **[BUG, gefunden & gefixt]** `MultiplayerManager.Disconnect()` (`async void`) hatte kein try/catch um `_lobby?.DeleteAsync()` — gleiches Muster wie der am 2026-08-22 Zyklus 17 gefixte `LobbyHandler.TickHeartbeat()`-Bug. Gefunden über projektweiten Sweep aller `async void`-Methoden (`grep -rn "async void" Assets/Scripts`). Anders als `TickHeartbeat` (läuft jeden Frame) wird `Disconnect()` nur einmalig beim Verlassen einer Lobby aufgerufen — geringeres Frequenzrisiko, aber bei Netzwerkfehler während des Löschens hätte eine unbehandelte Exception verhindert, dass `_lobby = null` und `SetState(State.Idle)` je erreicht werden, wodurch der Client dauerhaft im falschen State (`Connected`/`Hosting`) hängen bliebe. Fix: try/catch nach exakt dem bereits etablierten Muster im selben Namespace (`JoinPrivate`-Catch-Block direkt darüber als Vorlage). Kompiliert fehlerfrei (`isCompilationSuccessful: true`, Unity_RunCommand-Check nach dem Edit). NICHT im echten Multiplayer-Match mit simuliertem Netzwerkfehler getestet (bräuchte zweiten Client + gezielte Verbindungsunterbrechung während `DeleteAsync`, wie bereits beim Original-Fix vermerkt).

- **[QA, geprüft, kein Bug]** Projektweiter Sweep aller Dateien mit `PlayerPrefs.Set*`-Aufrufen (25 Dateien in `Assets/Scripts/`) auf fehlende `PlayerPrefs.Save()`-Aufrufe (analog zum am 2026-08-22 Zyklus 22 gefundenen `InAppReviewManager`-Bug). Ergebnis: **alle 25 Dateien rufen `PlayerPrefs.Save()` mindestens einmal auf** — keine weitere Instanz des damaligen Bug-Musters gefunden. Reiner statischer Text-Sweep (kein Play-Mode-Test, prüft nicht, ob `Save()` auf jedem Codepfad erreicht wird, nur ob der Aufruf überhaupt im File existiert).
- **[Blocker bestätigt, weiterhin offen]** `execute_blender_code_for_cli` weiterhin nicht nutzbar (`BLENDER_PATH` fehlt im MCP-Prozess) — erneut geprüft, unverändert seit 21.08. Interaktive Blender-GUI-Session hat aktuell ein anderes Objekt offen (`tripo_node_5e3f634c`, Workspace "UV Editing", nicht Teil eines bekannten Projekt-Assets) — zusätzlich zu `TapElement_Crystal.blend`, das laut `git status` weiterhin uncommitted ist und seit dem letzten Check neue Exporte (`.fbx`, mehrere Texturvarianten) bekommen hat. Beide GUI- und CLI-Wege für den Blender-Fallback sind damit diese Nacht blockiert.

- **[ERLEDIGT, Verifikation nachgeholt]** `ComboManagerTests` (5 Tests, aus Night Shift 2026-08-23, Commit `5b8eb24d`) über `TestRunnerApi` tatsächlich laufen lassen und Ergebnis per `Debug.Log`-Callback bestätigt: **5/5 Passed, 0 Failed** (`OnComboChanged_FiresWithCurrentStreakOnEachHit`, `RegisterHit_ColorChange_ResetsStreakToOne`, `RegisterHit_SameColorFiveTimes_ActivatesCombo`, `RegisterHit_SameColorFourTimes_ComboNotYetActive`, `RegisterMiss_DuringActiveCombo_BreaksCombo`). Schließt den offenen "Bitte von mir testen"-Punkt aus dem 2026-08-23-Report. Unity-MCP reagiert wieder normal (Compile-Check + Testlauf beide ohne Timeout durchgelaufen) — der 2026-08-23 gemeldete Blocker ist nicht mehr reproduzierbar.
- **[Beobachtung, WIP nicht angefasst]** Beim Start dieser Night Shift lag umfangreicher uncommitteter Stand vor, der über den bisher bekannten `TapElement_Crystal.blend`-WIP hinausgeht: `GameUIManager.cs`, `MixedPointSpawner.cs`, `GameOverSkinBinder.cs`, `SceneShortcuts.cs`, beide Haupt-Szenen (`GameScene_InfinityMode`, `MainMenuScene`), mehrere Shader/Materialien (Gritline, ToonShader) sowie `Packages/manifest.json`/`packages-lock.json` und `unity-neonsmash.slnx`. Bewusst nicht committet, editiert oder analysiert — sieht nach aktiver eigener manueller Arbeit aus (Shader-/Package-Änderungen deuten auf laufendes Experiment hin). Kein Code aus diesen Dateien wurde gelesen oder verändert.

## Hoch (kritisch/blockierend für Release oder bekannter Bug)

- **[TECHNISCHE SCHULD, bewusst nicht selbst angefasst, gefunden 2026-08-25]** Vollständiger Compiler-Warnungs-Sweep (`Unity_GetConsoleLogs` mit `logTypes=Warning`, 251 Zeilen, `grep` auf `warning CS[0-9]+`) zeigt **21 CS0618-Warnungen ("obsolete API")** im Projekt, davon **14 allein in `Assets/Scripts/Services/IAPManager.cs`** — die komplette Datei ist auf der alten Unity-IAP-v4-API aufgebaut (`IDetailedStoreListener`, `IStoreController`, `ConfigurationBuilder`). Die Warnung selbst verweist auf: *"Please upgrade to the new APIs introduced in IAP v5"* mit Link zum offiziellen Unity-Migrations-Guide (`docs.unity.com/ugs/en-us/manual/iap/manual/upgrade-to-iap-v5`). Funktioniert aktuell noch fehlerfrei (nur Warnung, kein Error), aber Unity entfernt deprecated APIs typischerweise in einer künftigen Package-Version — ein zukünftiges Unity-IAP-Package-Update könnte das komplette Kaufsystem ohne Migration brechen. **Bewusst nicht selbst migriert:** Eine IAP-v5-Migration ist eine substanzielle, risikoreiche Änderung an echtem Zahlungscode, die echtes Sandbox-Testing mit dem Store braucht — nicht als isolierter Night-Shift-Fix geeignet. Restliche 7 Warnungen kleiner/verteilt: `GcGamertagToUgs.cs` (5×, veraltete `UnityEngine.SocialPlatforms.Social`-API für Game Center, seit Jahren von Unity als deprecated markiert), `AdManager.cs` (1×), `DevicePerformance.cs` (1×) — geringere Priorität, da nicht zahlungsrelevant. **Empfehlung:** Als eigenständiges, geplantes Arbeitspaket (kein Night-Shift-Nebenbei-Fix) mit ausreichend Zeit für echtes Sandbox-Kauf-Testing einplanen, bevor Unity die v4-API tatsächlich entfernt.

- **[KRITISCH, bitte prüfen, gefunden 2026-08-25]** Die aktive `applicationIdentifier` (Bundle-ID) für iPhone in `ProjectSettings/ProjectSettings.asset` (Zeile ~175, die tatsächlich wirksame globale PlayerSettings-Datei, nicht die inerte `m_PlayerSettingsYaml`-Kopie in den Build Profiles — dieselbe Unterscheidung wie beim bereits am 2026-08-20 geklärten `NEONSMASH_PROD`-Fall) ist aktuell `com.sheronyx.neonsmash.dev`. Per `git log -p` auf diese Datei bestätigt: Das war ursprünglich `com.sheronyx.neonsmash` (ohne `.dev`) und wurde in der Historie mindestens zweimal auf `.dev` geändert — vermutlich für lokales/Dev-Testing. Android hat denselben Aufbau (`com.sheronyx.neonsmash.dev` für Android in derselben Datei). **Kann nicht selbst verifiziert werden, ob das ein Fehler ist** — es ist möglich, dass `com.sheronyx.neonsmash.dev` bereits die tatsächlich im App Store/Play Store registrierte, "echte" Bundle-ID ist (dann kein Problem) oder dass vor jedem echten Store-Build manuell auf `com.sheronyx.neonsmash` zurückgewechselt werden muss (dann JETZT ein Blocker, falls vergessen). **Bitte vor dem nächsten Store-Build unbedingt gegen die tatsächlich in App Store Connect/Play Console registrierte Bundle-ID abgleichen** — eine falsche Bundle-ID beim Build lässt sich nach dem ersten Store-Submit nicht mehr ändern, ohne eine komplett neue App-Store-Eintragung anzulegen.

- **[KRITISCH, Release-Blocker, gefunden 2026-08-25]** Das iOS "App Store"-Marketing-Icon-Slot (1024×1024, "iOS Kind: 2" in `ProjectSettings/ProjectSettings.asset` Zeile ~430ff) ist auf `Assets/Images/GooglePlayStore/appIcon512px.png` gesetzt — ein **Google-Play-Icon (512×512)**, nicht ein dediziertes iOS-1024er-Asset. Zwei konkrete Probleme: (1) Falsche Plattform-Quelle, wird von 512 auf 1024 hochskaliert (Qualitätsverlust). (2) **Die Datei hat einen Alpha-Kanal** (`sips -g hasAlpha` → `yes`, per Python/PIL nicht weiter analysierbar, da `PIL` in dieser Umgebung fehlt — aber `sips` reicht als Bestätigung). App Store Connect **lehnt 1024×1024-Marketing-Icons mit Alpha-Kanal beim Upload automatisch ab**, unabhängig davon, ob tatsächlich transparente Pixel sichtbar sind — das ist ein bekanntes, garantiertes Apple-Validierungsproblem, kein Vermutung. Visuell geprüft (Bild geöffnet): Icon-Design selbst ist ordentlich (Logo-Explosion, dunkler Hintergrund), nur das Dateiformat ist das Problem. Es existiert bereits eine separate `Assets/Images/Logo/AppIcon 1024.png` (1024×1024, ebenfalls RGBA mit Alpha — also vermutlich ebenso betroffen, falls die als Ersatz verwendet würde) sowie `Assets/Images/AppStore/AppStore_Icon_fading.png` (922×923 — falsches Seitenverhältnis/keine 1024×1024, für den Marketing-Slot ungeeignet). **Bewusst nicht selbst gefixt:** Bild-Alpha-Kanal entfernen + `ProjectSettings.asset`/PlayerSettings-Icon-Zuweisung ändern ist eine Store-Submission-kritische Änderung, die ich ohne visuelle Freigabe des finalen Ergebnisses nicht eigenständig vornehmen sollte (Gefahr: falsches Icon final einreichen). **Konkrete Empfehlung für den nächsten Schritt:** In Unity → Player Settings → iOS → Icon → "App Store" (1024×1024)-Slot ein dediziertes Asset ohne Alpha-Kanal zuweisen (z. B. `AppIcon 1024.png` nach Alpha-Entfernung, oder `AppStore_Icon_fading.png` auf 1024×1024 zugeschnitten). Gehört in die Release-Checkliste (siehe Memory `project_release_checklist`) — noch nicht dort vermerkt, hiermit ergänzt.

- **[BUG, gefunden & gefixt 2026-08-22 Zyklus 17]** `LobbyHandler.TickHeartbeat()` (Multiplayer, `async void`, läuft jeden Frame) hatte kein try/catch um den Heartbeat-Call — bei abgelaufener Lobby/Netzwerkfehler während eines Matches hätte das alle 15s eine unbehandelte Exception geworfen. Fix analog zum bereits vorhandenen `DeleteAsync`-Muster im selben File, Commit `f7ab50d1`. Kompiliert fehlerfrei; NICHT live in einem echten Multiplayer-Match getestet (bräuchte zweiten Client + gezielte Netzwerkunterbrechung).
- **[KRITISCHER BUG, gefunden & gefixt 2026-08-22 Zyklus 14, live verifiziert Zyklus 29]** `DiamondManager`/`DiamondSplinterManager` wurden nie beim App-Start aus der Cloud geladen (`UgsBootstrap.cs` rief nur Tutorial/DreamEnergy/DailyReward/Achievement). Ein Geräte-/Reinstall-Wechsel hat den kompletten Diamanten-/Splitter-Stand verloren — beide sind aktive Ingame-Währung (`ShopInventory.cs`, `IAPManager.cs`). Fix: beide `LoadFromCloudAsync()`-Aufrufe ergänzt, Commit `a0b8ecd5`. Zyklus 29: `BootstrapScene` mit echter UGS-Verbindung (Anonymous Sign-In) in Play Mode durchlaufen — beide Load-Aufrufe liefen fehlerfrei durch den echten Ablauf, 0 Errors. Damit über reine try/catch-Resilienz hinaus bestätigt.

- **[BUG, gefunden & gefixt 2026-08-21 Zyklus 6]** Pause während Life-Lost-Ramp-Up hob die Pause faktisch wieder auf — `LivesManager.RampUpTimeScale()` überschrieb `Time.timeScale` weiter, während `PauseMenuController.ShowPauseMenu()` es auf 0 gesetzt hatte. Fix: `LivesManager.CancelRampUp()`, aufgerufen aus `ShowPauseMenu()`. Commit `b4fe313b`. Live im Play Mode verifiziert (Unity-MCP, frame-genaues Polling über 60 Frames, kein Drift mehr). Kein automatisierter PlayMode-Test ergänzt — EditMode kann Coroutine-Timing nicht abbilden.
- **[BUG, gefunden & gefixt 2026-08-21 Zyklus 7, live verifiziert Zyklus 8]** Identisches Muster in `SequenceManager.Co_PlayEffect()` gefunden (gezielte Suche nach allen `Time.timeScale`-Zuweisungen im Projekt) — Combo-Freeze-Frame setzte `Time.timeScale=1f` nach Ablauf unconditional zurück, hätte eine während des Freeze aktive Pause aufgehoben. Fix: Guard `if (!PauseMenuController.IsPaused)`, Commit `7b2dadef`. Live im Play Mode nachgestellt (Unity-MCP, temporäres `SequenceDefinition` mit 1-Schritt-Sequenz + Dummy-Combo-Prefab, `RegisterHit` löst Freeze aus, Pause währenddessen simuliert): über 181 Frames nach `ShowPauseMenu()` blieb `timeScale=0`, obwohl `effectDuration` (1s) längst abgelaufen war — Fix bestätigt.

- **[BUG-Beschreibung korrigiert, geprüft 2026-08-20 Zyklus 3]** `NEONSMASH_PROD` Define zwischen den Build-Profile-Assets: Bei Dateiinspektion (Unity lief, `Unity_GetConsoleLogs` bestätigte keine Fehler) zeigte sich, dass der ursprünglich gemeldete "Vertausch" in `Assets/Settings/Build Profiles/Android Release.asset` / `iOS.asset` tatsächlich in `m_PlayerSettingsYaml` liegt — das ist nur eine inerte Snapshot-Kopie alter globaler PlayerSettings, **nicht** die aktive Konfiguration (beide Profile haben `m_HasScriptingDefines: 0` und `m_ScriptingDefines: []`, überschreiben also nichts). Die tatsächlich wirksame Quelle ist `ProjectSettings/ProjectSettings.asset` → `scriptingDefineSymbols` (global, ein Eintrag pro Plattform). Dort ist **aktuell weder für Android noch iPhone `NEONSMASH_PROD` oder `NEON_ANALYTICS_TEST` gesetzt** — laut Code-Konvention (`AdConfig.cs`, `UgsBootstrap.cs`: "ohne Define → development") ist das der sichere Default, aber es bedeutet: vor jedem echten Store-Release muss `NEONSMASH_PROD` manuell in Player Settings → Scripting Define Symbols für die Zielplattform gesetzt werden — es gibt keinen Automatismus und keinen Bug, der das verhindert. Alte Formulierung ("vertauscht") war irreführend und ist hiermit ersetzt. Bleibt Release-Checkliste-Punkt (siehe `project_release_checklist`), kein Code-Bug mehr.
- **[OFFEN]** iOS AdMob Interstitial — letzter bekannter Stand: Propagation/Account-seitig, Code als korrekt verifiziert. Bei nächster Gelegenheit mit echtem Gerät erneut testen (siehe `project_admob_plan`).

## Vom User priorisiert (2026-08-21, für nächste Night Shift)

- **[TEILWEISE ERLEDIGT, Night Shift 2026-08-22 Zyklus 15]** Icon-Artwork selbst erstellt (`Assets/001 Fairy World/Elements/Fairy Progress Icons/`, Commit `77df1e68`) — Blatt/Tropfen/Kristall als 128x128 Sprites, importiert und konfiguriert. **Weiterhin blockiert:** die eigentliche Verdrahtung in der Szene (Icon-GameObjects neben den Fill-Bars in `ColorProgressUI`/`GameScene_InfinityMode.unity`) — Szene hat weiterhin einen großen uncommitteten WIP-Diff (manuelle 3D-Charakter-Arbeit), wird laut Standing-Regel nicht angefasst. Sobald WIP geklärt: Icons sind fertig zum Drag&Drop in die Image-Felder.
- **[TEILWEISE ERLEDIGT, Night Shift 2026-08-22 Zyklus 18]** Reward-Preview: Code-Unterstützung in `ColorProgressUI.cs` (optionale, nullable Felder + Schwellenwert-Logik) und Sparkle-Icon erstellt, Commit `7356ea98`. **Weiterhin blockiert:** Zuweisung der Icons in der Szene selbst (`GameScene_InfinityMode.unity`, weiterhin uncommitteter WIP).
- **[ERLEDIGT, Night Shift 2026-08-21]** Streak-Freeze für `DailyRewardManager` umgesetzt — reiner Code-Task, keine Szene betroffen. Siehe Commit `4180f37d`. UI-Anbindung (Anzeige der Charges, Freeze-Button) bleibt offen, ebenfalls Scene-WIP-blockiert.

## Mittel (hoher Nutzen, vertretbarer Aufwand)

- **[BUG, gefunden 2026-08-19, GEFIXT 2026-08-20]** `DailyRewardManager`: Reward-Index clampte bei 7 statt zu wrappen → Streak blieb ab Tag 7 für immer bei Reward-Stufe 7 hängen. Fix in Commit `2c169a7a`: neue `RewardTierIndex()`-Funktion wrapped den Reward-Index per Modulo, der rohe Streak-Zähler (PlayerPrefs/Cloud, für `AchievementManager.OnStreakReached`) bleibt unclamped/monoton wachsend — einzige bestehenden Streak-Achievements sind Streak3/Streak7, unverändertes Verhalten dort. Design-Entscheidung: Zyklus und Rohzähler trennen statt beide zu wrappen.
- **[ERLEDIGT, Night Shift 2026-08-21 Zyklus 4]** `DreamEnergyManager`-Cloud-Save-Drift behoben — Dirty-Flag + Retry bei `LoadFromCloudAsync` (App-Resume), Commit `53e0ba17`. War zuvor als "kein Quick-Fix" zurückgestellt, ließ sich bei genauerer Betrachtung aber klein und isoliert umsetzen (kein UI/Scene nötig). 11/11 EditMode-Tests grün.
- **[ERLEDIGT, Night Shift 2026-08-21 Zyklus 10]** Gleicher Cloud-Save-Drift-Bug auch in `DiamondManager` und `DiamondSplinterManager` gefunden (identische Speicher-/Sync-Struktur) und mit demselben Dirty-Flag+Retry-Muster gefixt, Commit `0c5886f3`. Funktional im Editor verifiziert (Fehlschlag→Dirty-Flag→Retry→No-Op bei sauberem Zustand), echter Spielerstand danach unverändert wiederhergestellt.
- **[ERLEDIGT, Night Shift 2026-08-21 Zyklus 11]** Sweep abgeschlossen: `AchievementManager` (letzter verbliebener Manager mit diesem Muster, zwei unabhängige Save-Pfade) ebenfalls gefixt, Commit `44b5d8a4`. `TutorialProgress.cs` bewusst ausgenommen — funktional tot (`IsTutorialCompleted` hart auf `true`). Alle vier betroffenen Manager (DreamEnergy/Diamond/DiamondSplinter/Achievement) haben jetzt konsistent Dirty-Flag+Retry.
- **[ERLEDIGT, Night Shift 2026-08-21 Zyklus 12]** Automatisierte EditMode-Tests für die drei zuvor nur manuell verifizierten Cloud-Dirty-Fixes nachgezogen (Diamond/DiamondSplinter/Achievement), Commit `dbf9b251`. 9/9 grün, Test-Isolation bestätigt (Realstand vor/nach unverändert). Gesamte Test-Suite jetzt: 8 (DailyReward) + 3 (DreamEnergy) + 9 (Diamond/Splinter/Achievement) = 20 EditMode-Tests für die Services-Schicht.
- **[Test-Isolations-Bug, gefunden & gefixt 2026-08-21 Zyklus 4]** `DailyRewardManagerTests.cs` (aus Zyklus 2) hat `DreamEnergyManager`-PlayerPrefs nicht gesichert/wiederhergestellt — jeder Testlauf hat den echten Editor-`dream_energy_balance` dauerhaft erhöht (auf 50100 aufgelaufen, keine Baseline vor Zyklus 2 aufgezeichnet, daher nicht mehr exakt rückrechenbar). Fix in Commit `53e0ba17`. **Für den User:** falls der Editor-PlayerPrefs-Dream-Energy-Stand (50100) nicht dem erwarteten Testdaten-Stand entspricht, manuell über `PlayerPrefs.DeleteKey("dream_energy_balance")` im Editor zurücksetzen — betrifft nur lokale Editor-Testdaten, keine echten Spielerdaten.

- ~~Streak-Freeze für `DailyRewardManager`~~ — siehe "Vom User priorisiert" oben, Backend erledigt in Night Shift 2026-08-21 (Commit `4180f37d`), UI-Teil bleibt dort als offener Punkt.
- **[ERLEDIGT, Night Shift 2026-08-21 Zyklus 2]** EditMode-Tests für `DailyRewardManager` ergänzt (`Assets/Editor/Tests/DailyRewardManagerTests.cs`, Commit `c2090fb5`) — 8/8 Tests grün über `TestRunnerApi` im laufenden Editor verifiziert, deckt Streak-Freeze, Reward-Tier-Wrap und Already-Claimed-Fall ab.
- **[Beschreibung korrigiert, geprüft 2026-08-20 Zyklus 4]** `PhaseManager`-GameObject in `GameScene_InfinityMode`: entgegen dem alten Backlog-Text ("fehlt noch") ist das GameObject "Phase Manager" tatsächlich bereits in der Szene vorhanden und aktiv (`m_IsActive: 1`, `m_Enabled: 1`, `spawner`-Referenz gesetzt, Zeile ~2130 in `GameScene_InfinityMode.unity`). Per Live-Play-Mode-Test via Unity-MCP bestätigt: `PhaseManager.Instance` ist zur Laufzeit nicht null, ein `PhaseManager`-Component existiert genau einmal in der Szene. Vermutlich zwischenzeitlich durch manuelle User-Arbeit ergänzt (kein Night-Shift-Commit dafür auffindbar). Das 12-Phasen-System ist damit **nicht mehr inaktiv** — falls das unbeabsichtigt ist, sollte der User das prüfen, da es Balance/Flow direkt beeinflusst. Kein Handlungsbedarf durch Night Shift, nur Beobachtung/Korrektur.
- **[Beschreibung korrigiert, geprüft 2026-08-20 Fortsetzung Zyklus 6]** `DiamondPoint`-Prefab: entgegen dem alten Backlog-Text ("bauen") existiert `Assets/Prefabs/Diamond Point Collectable.prefab` bereits vollständig — `SpriteRenderer`, `CapsuleCollider2D`, sowie die Komponenten `DiamondPoint`, `PointPulse` und `SpawnGrowTarget` (per GUID-Abgleich in `Assets/Scripts/Gameplay/Logic/` verifiziert). Das Prefab ist zudem in `Assets/Scenes/GameScenes/GameScene_InfinityMode.unity:3173` als `diamondPrefab`-Feld verdrahtet (GUID `3a9f3f06d50854d3db2b00f79c831aa8` referenziert). Punkt kann aus dem Backlog, kein Handlungsbedarf. Vermutlich zwischenzeitlich durch manuelle User-Arbeit ergänzt.
- **[Erledigt/geprüft 2026-08-20 Zyklus 3]** Diagnose-Logs in `AdManager.cs` erneut geprüft: aktuell 8 `Debug.Log`/`Debug.LogWarning`-Aufrufe, alle mit `[Ads]`-Präfix, ausschließlich Fehler-/Statuswechsel-relevant (Consent-Fehler, Ad-Laden fehlgeschlagen, Init-Status) — kein Rauschen, kein Handlungsbedarf. Punkt kann aus dem Backlog.

## Niedrig / Recherche / Design (kein Blocker)

- **[Idee, 2026-08-22]** `life_lost`/Miss-Rate-Analytics (Routine Abschnitt 22 nennt "Tap-Fehlerrate; Swipe-Fehlerrate" explizit als Messwert). Aktuell komplett ungetrackt. Bewusst NICHT umgesetzt: `LivesManager.LoseLife()` wird von vielen verschiedenen Special-Mode-Skripten ohne "Ursache"-Parameter aufgerufen — eine sinnvolle Tap-vs-Swipe-Aufschlüsselung bräuchte Signatur-Änderungen an mehreren Call-Sites, was mehr Produktentscheidung (welche Granularität gewünscht?) als reiner Bugfix ist. Nur ein reines "life_lost"-Zählevent ohne Ursache wäre wenig aussagekräftig. Als Idee vermerkt statt spekulativ umgesetzt.

- **[QA-Fund, nicht night-shift-verursacht, 2026-08-22 Zyklus 18]** `Assets/Bundles/01 Default/Prefabs/Top Bar Default.prefab` wirft bei jedem Reimport "Transform child can't be loaded"/"Immediate cast failed from GameObject to Transform" (12x). Datei ist seit 20.08. unverändert (nicht Teil des heutigen WIP, nicht von mir angefasst) — vermutlich alte verwaiste FileID-Einträge aus einer früheren Bearbeitung. Prefab lädt trotzdem korrekt (3 Kinder erhalten), funktional aktuell kein bekannter Schaden. Nicht selbst repariert (YAML-Chirurgie an fremdem WIP-Bundle-Prefab zu riskant ohne genaue Kenntnis der beabsichtigten Struktur) — bei Gelegenheit im Editor öffnen und einmal manuell speichern lassen, das bereinigt solche verwaisten Einträge meist automatisch.

- **[Vorschlag, Recherche 2026-08-22]** Soft-Ask-Screen vor der nativen Notification-Permission-Anfrage — aktuell fragt `FirebaseNotificationManager` direkt beim allerersten App-Start ohne Kontext, laut Recherche der ungünstigste Zeitpunkt. Siehe `docs/RESEARCH.md` Abschnitt "Push-Notification-Opt-In-Timing". Braucht neue UI-Komponente + Timing-Entscheidung, kein Quick-Fix.
- **[Vorschlag, Recherche 2026-08-22]** Zeitlich begrenztes Leaderboard (täglich/wöchentlich) zusätzlich zu den bestehenden 2 All-Time-Boards — siehe `docs/RESEARCH.md` Abschnitt "Leaderboard-Design für Retention". Braucht neue UGS-Leaderboard-IDs + Reset-Logik, kein Quick-Fix.
- **[Vorschlag, Recherche 2026-08-22]** Dritte Diamant-IAP-Stufe (~15–20 Diamanten) zwischen den bestehenden 5er/50er-Paketen als Decoy-Option ergänzen — siehe `docs/RESEARCH.md` Abschnitt "IAP-Pricing-Patterns". Braucht neues Store-Produkt (App Store Connect/Play Console) + neues `ShopItem`-Asset, ist eine Preis-/Business-Entscheidung, keine Night-Shift-Aufgabe.

- Flow/Pacing-Redesign (Special Modes als Ruhephase, Ease-Ramp bei Phasenwechseln) — Diskussionsstand vom 2026-07-29, noch nicht umgesetzt. Braucht zuerst Klärung: gibt es reales Playtester-Signal für "zu stressig"?
- Magnet Mode (echte Mechanik statt Gravity-Platzhalter für Grün) — späteres Feature.
- Phase 13 Endless-Feindesign — bewusst zurückgestellt.
- 3D-Tap/Swipe-Elemente (ob Umstellung auf 3D für Gameplay-Elemente selbst sinnvoll ist) — als riskant geflaggt, braucht Prototyp/Test vor Entscheidung, nicht nur Fairy-Skins.

- **[Idee, Recherche 2026-08-21]** Reward-Preview am Feen-Energie-Füllbalken (`ColorProgressUI.cs`): laut UI-Pattern-Recherche sollte ein Fortschrittsbalken andeuten, was bei Erreichen des Ziels wartet, statt nur reinen Füllstand zu zeigen. NeonSmashs 3 Feen-Balken zeigen aktuell nur Fortschritt ohne Preview auf den Special-Mode-Trigger. Kleines Icon/Glow am Balkenende als möglicher nächster Schritt — siehe `docs/RESEARCH.md` Abschnitt "UI-Pattern für Progress-/Meta-Progression-Bars". Kein Quick-Fix (UI + Szene), daher nicht umgesetzt.

## Aus Night Shift 2026-08-22 (Zyklus 34) hinzugekommen

- **[Konsistenzprüfung]** Zwischen-Check nach 52 Commits dieser Night Shift: WIP-Szenen (`DemoDesignScene`, `GameScene_InfinityMode`, `MainMenuScene`) weiterhin exakt im Ausgangszustand (nur `M`, keine neuen Änderungen), vollständige EditMode-Testsuite (alle Assemblies, nicht nur die eigenen 4 Testklassen) 20/20 grün.

## Aus Night Shift 2026-08-22 (Zyklus 37) hinzugekommen

- **[Geklärt]** Die seit langem offene "15 vs. 20 Energie-Trigger"-Frage (siehe Memory `project_dream_world_fairies_concept`) ist aufgelöst: `colorTriggerThreshold` in `GameScene_InfinityMode.unity` (`PhaseManager`) steigt progressiv 10→15→20 über die 12 Phasen (je 4 Phasen pro Stufe) — kein fixer Einzelwert, beide alten Zahlen waren für unterschiedliche Spielphasen korrekt. Rein per Datei-Lesen verifiziert (keine Szenenänderung).

## Aus Night Shift 2026-08-22 (Zyklus 35) hinzugekommen

- **[ERLEDIGT, Fallback-Blender-Design]** `IceShard_Hazard` erstellt — erstes Gefahren-Konzept (nicht Collectible) dieser Night Shift, Commit `a5997e9a`. In Unity importgeprüft (fehlerfrei).

## Aus Night Shift 2026-08-22 (Zyklus 32) hinzugekommen

- **[ERLEDIGT, Cleanup]** Totes Duplikat `LeaderboardIds.cs` entfernt (definierte `infinity_highscore` ein zweites Mal, unbenutzt neben `LeaderboardApi.InfinityId`). Commit `1e53e6f2`.

## Aus Night Shift 2026-08-22 (Zyklus 31) hinzugekommen

- **[ERLEDIGT, stärkste Verifikation]** Gleicher Erfolgsfall-Test mit echter Cloud-Verbindung auch für `DiamondManager`, `DiamondSplinterManager` und `AchievementManager` durchgeführt (Fortsetzung Zyklus 30) — alle vier Dirty-Flags nach echtem Save auf 0, Cloud-Logs bestätigen tatsächliche Speicherung ("Cloud gespeichert: 5"/"10"). Damit sind jetzt ALLE VIER Cloud-Dirty-Fixes dieser Night Shift end-to-end mit echtem Backend verifiziert, nicht nur der Fehlerpfad im Editor-Mock.

## Aus Night Shift 2026-08-22 (Zyklus 30) hinzugekommen

- **[ERLEDIGT, stärkste Verifikation]** Dirty-Flag-Retry-Mechanismus (`DreamEnergyManager.RetryPendingCloudSaveIfNeeded`) mit ECHTER UGS-Cloud-Verbindung im Erfolgsfall getestet (nicht nur Fehlerfall wie zuvor): Dirty-Flag manuell auf 1 gesetzt, Retry aufgerufen → echter Cloud-Save gelang, Flag korrekt auf 0 zurückgesetzt. Log bestätigt: "[DreamEnergy] Cloud gespeichert: 50100". Damit ist der Kernmechanismus aller vier Cloud-Dirty-Fixes (Zyklus 4/10/11) jetzt End-to-End mit echtem Backend bestätigt, nicht nur die Fehlerpfad-Resilienz.

## Aus Night Shift 2026-08-22 (Zyklus 29) hinzugekommen

- **[ERLEDIGT, Play-Mode-Verifikation]** `BootstrapScene` end-to-end in Play Mode getestet (echte UGS-Verbindung, kein Editor-Mock): Anonymous Sign-In erfolgreich, `DiamondManager`/`DiamondSplinterManager.LoadFromCloudAsync()` (heutiger kritischer Fix aus Zyklus 14) liefen ohne Fehler durch den echten Bootstrap-Ablauf, 0 Errors insgesamt. Transiente "2 EventSystems/2 AudioListeners"-Warnung beim Bootstrap→MainMenu-Übergang beobachtet, aber selbstheilend (Endzustand nach Übergang: exakt 1 von jedem) — kein anhaltender Bug, nicht weiterverfolgt.

## Aus Night Shift 2026-08-22 (Zyklus 24) hinzugekommen

- **[BUG, gefunden & gefixt]** `DebugResetPrefs.cs` (Strg+R löscht `PlayerPrefs.DeleteAll()`) fehlte der `#if UNITY_EDITOR || DEVELOPMENT_BUILD`-Guard, den `RewardDebugHelper.cs` für dieselbe Art Aktion bereits hat. Reales Risiko, falls das Skript je wieder einem GameObject zugewiesen wird und in einen Production-Build landet. Aktuell inert (nicht referenziert), Guard trotzdem ergänzt statt nur zu löschen. Commit `cb693b51`.

## Aus Night Shift 2026-08-22 (Zyklus 22) hinzugekommen

- **[ERLEDIGT]** `InAppReviewManager` speicherte PlayCount/ReviewShown nie mit `PlayerPrefs.Save()` — bei App-Crash/Kill vor dem nächsten automatischen Flush wäre der Status verloren gegangen. Fix, Commit `27cc234b`. Niedrige Kritikalität (keine Spielerwährung betroffen).

## Aus Night Shift 2026-08-22 (Zyklus 21) hinzugekommen

- **[ERLEDIGT]** Ads/IAP-Analytics-Lücke geschlossen — `AdManager`/`IAPManager` riefen `NeonAnalytics` nirgends auf (kein Rewarded-Completion/Interstitial/Purchase-Tracking). Neue Events ergänzt, Commit `92759162`. Kompiliert fehlerfrei nach Fix eines falschen Firebase-Parameternamens, 20/20 Tests weiterhin grün. **Bitte bei Gelegenheit mit echtem Firebase-DebugView verifizieren** (Editor kann Firebase Analytics nicht wirklich senden).

## Aus Night Shift 2026-08-22 (Zyklus 20) hinzugekommen

- **[ERLEDIGT, Fallback-Blender-Design]** `GoldStar_Bonus` erstellt — farbneutrales Bonus-Collectible (nicht an Fee-Farbe gebunden), Commit `96a034bf`. In Unity importgeprüft (fehlerfrei). Noch nicht in `DemoDesignScene.unity` platziert (gleicher Grund wie LeafGem/StoneShard).

## Aus Night Shift 2026-08-22 (Zyklus 19) hinzugekommen

- **[ERLEDIGT, Cleanup]** Verwaistes Diagnose-Skript `DebugTimeScaleLogger.cs` entfernt — selbst als "nach Diagnose entfernen" markiert, zugehöriger Bug laut `FairyWingFlap.cs`-Kommentaren bereits behoben, nirgends mehr referenziert. Commit `dac2aec6`.

## Aus Night Shift 2026-08-21 (Zyklus 3) hinzugekommen

- **[ERLEDIGT, Fallback-Blender-Design]** `LeafGem_Boost` erstellt — Grün-Pendant zu `CrystalShard_Boost`, siehe Commit `2fcde0ea`. In Unity importgeprüft (fehlerfrei). **Offen:** Platzierung in `DemoDesignScene.unity` — Szene hat aktuell eine kleine uncommittete manuelle Änderung (Objekt-Rotation), daher nicht angefasst. Nachtrag sobald WIP geklärt ist.
- **[ERLEDIGT, Fallback-Blender-Design, Zyklus 9]** `StoneShard_Boost` erstellt — Pink/Gestein-Pendant, vervollständigt das Boost-Trio (Grün/Blau/Pink), Commit `9ef206bd`. In Unity importgeprüft (fehlerfrei). Gleiches Offen-Item wie LeafGem_Boost: Platzierung in `DemoDesignScene.unity` steht noch aus.
- **[Blocker, technisch]** `execute_blender_code_for_cli` (Headless-Blender-MCP-Tool) ist nicht nutzbar — `BLENDER_PATH`-Umgebungsvariable ist im MCP-Serverprozess nicht gesetzt, obwohl `Blender.app` unter `/Applications/Blender.app` existiert. Für zukünftige Blender-Fallback-Aufgaben ohne offene GUI-Session müsste das gesetzt werden (liegt außerhalb dessen, was ich als Night Shift selbst ändern kann/soll — Konfiguration des MCP-Servers, kein Projekt-Code).

## Aus Night Shift 2026-08-23 hinzugekommen

- **[QA, geprüft, kein Bug]** Alle `Time.timeScale = ...`-Zuweisungen im Projekt erneut vollständig durchsucht (Fortsetzung des Sweeps aus Zyklus 7 der 2026-08-21-Night-Shift, der `SequenceManager` fixte). `GameUIManager.RestartGame()`/`BackToMenu()` setzen `Time.timeScale = 1f` unconditional, aber das ist kein Bug: beide sind explizite User-Buttons auf dem Game-Over-Screen und laden direkt danach eine neue Szene — kein Fall, in dem eine parallel aktive Pause überschrieben werden könnte. Kein Handlungsbedarf.
- **[Blocker bestätigt, weiterhin offen]** `execute_blender_code_for_cli` weiterhin nicht nutzbar (`BLENDER_PATH` fehlt im MCP-Prozess, siehe Eintrag unten). Zusätzlich: die interaktive Blender-GUI-Session hatte in dieser Night Shift `TapElement_Crystal.blend` offen (erkennbar als eigenständige, nie committete User-Arbeit unter `Assets/001 Fairy World/Elements/3D Elements/`) — bewusst nicht angefasst, auch nicht lesend über den `bpy`-Datenkontext hinaus. Blender-Fallback-Aufgaben (neue Designs) sind diese Nacht dadurch blockiert, solange die GUI-Session mit fremdem WIP belegt ist.
- **[Beobachtung, kein Handlungsbedarf]** Leerer Ordner-Stub `Assets/001 Fairy World/3D Concepts/PrismShard_Boost/` (nur `.meta`, kein Inhalt, seit 21.08. nie committet) gefunden — vermutlich ein abgebrochener Anlauf für ein 6. Boost-Design, das nie fertiggestellt wurde. Nicht selbst befüllt (Blender-GUI blockiert, siehe oben) und nicht gelöscht (unklar, ob User das absichtlich als Platzhalter für eigene Arbeit angelegt hat). Bei nächster freier Blender-GUI-Session: entweder PrismShard_Boost-Design tatsächlich erstellen oder Stub-Ordner aufräumen.

## Aus Night Shift 2026-08-19 hinzugekommen

Siehe [reports/nightly/2026-08-19.md](../reports/nightly/2026-08-19.md) für Details zu Findings aus Code-QA-Scan und Recherche dieser Nacht.
