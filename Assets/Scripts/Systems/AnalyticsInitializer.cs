using UnityEngine;

public class AnalyticsInitializer : MonoBehaviour
{
    // Nicht mehr nötig: UgsBootstrap initialisiert Services,
    // ConsentManager startet Analytics nach Zustimmung.
    void Awake() { /* intentionally empty */ }
}
