using UnityEngine;

// UNGENUTZT seit PointFlyIn auf reine Scale-Pop-Spawn-Animation umgestellt wurde (wächst jetzt das
// Root-Objekt direkt, keine einzeln markierten Kind-Objekte mehr). Bewusst nicht gelöscht, um
// "Missing Script" auf Prefabs zu vermeiden, die diese Komponente noch gesetzt haben — kann dort in
// Ruhe manuell entfernt werden, ist ansonsten inert.
public class SpawnGrowTarget : MonoBehaviour
{
}
